package com.reachlocal.mobile.liger.listeners;

import com.reachlocal.mobile.liger.ui.PageFragment;

/**
 * Interface that objects that need to listen for RootPageFragment events.
 */
public interface RootPageListener {

    public void onFragmentFinished(PageFragment page);

}
